# MomentKeep 微信云托管部署说明

## 一、Dockerfile 说明

本项目使用多阶段构建的 Dockerfile，优化镜像大小和构建效率：

### 构建阶段
- 基础镜像：`maven:3.9-amazoncorretto-21-alpine`
- 包含 JDK 21 和 Maven 3.9
- 执行 Maven 构建命令，跳过测试

### 运行阶段
- 基础镜像：`amazoncorretto:21-alpine`（轻量级 JDK 21 运行环境）
- 设置上海时区（Asia/Shanghai）
- 配置 JVM 参数优化容器运行
- 暴露端口：8080

## 二、微信云托管配置

### 1. 服务配置

在微信云托管控制台创建服务时，需要配置以下参数：

**基本信息**：
- 服务名称：momentkeep-backend
- 服务类型：Web 服务
- 计费方式：按量付费或包年包月

**容器配置**：
- CPU：建议 0.5核 - 1核
- 内存：建议 512MB - 1GB
- 最小实例数：0（按量付费）或 1（包年包月）
- 最大实例数：根据业务需求配置

### 2. 环境变量配置

在微信云托管控制台的「环境变量」配置中，需要添加以下环境变量：

#### 必需环境变量

```bash
# 数据库配置（TiDB Cloud）
SPRING_DATASOURCE_URL=jdbc:mysql://gateway01.ap-southeast-1.prod.alicloud.tidbcloud.com:4000/momentkeep?ssl=true
SPRING_DATASOURCE_USERNAME=3M8M5Vr1pf71qDH.root
SPRING_DATASOURCE_PASSWORD=KKrZhh0RGEMUq7k6

# JWT 配置
JWT_SECRET_KEY=your-secret-key-here
JWT_EXPIRATION_TIME=86400000

# DeepSeek AI 配置
DEEPSEEK_API_KEY=your-deepseek-api-key
DEEPSEEK_API_URL=https://api.deepseek.com

# 缤纷云存储配置（S3）
BINFENGYUN_ACCESS_KEY=your-access-key
BINFENGYUN_SECRET_KEY=your-secret-key
BINFENGYUN_BUCKET_NAME=momentkeep
BINFENGYUN_ENDPOINT=https://s3.bitiful.net

# Redis 配置（如果使用微信云托管的 Redis）
SPRING_DATA_REDIS_HOST=redis-service-name
SPRING_DATA_REDIS_PORT=6379
SPRING_DATA_REDIS_PASSWORD=redis-password

# Spring Profile
SPRING_PROFILES_ACTIVE=prod
```

#### 可选环境变量

```bash
# JVM 参数（已在 Dockerfile 中配置，可根据需要调整）
JAVA_OPTS=-Xms256m -Xmx512m -XX:+UseContainerSupport

# 服务器端口（默认 8080）
SERVER_PORT=8080
```

### 3. 应用配置文件调整

建议创建 `application-prod.yml` 用于生产环境：

```yaml
spring:
  application:
    name: MomentKeep
  
  # 数据库配置（从环境变量读取）
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      max-lifetime: 30000
      validation-timeout: 3000
      idle-timeout: 60000
      connection-test-query: SELECT 1
  
  # Redis 配置（从环境变量读取）
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST}
      port: ${SPRING_DATA_REDIS_PORT}
      password: ${SPRING_DATA_REDIS_PASSWORD}
  
  servlet:
    multipart:
      max-file-size: 10MB

server:
  port: ${SERVER_PORT:8080}

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: cn.edu.scnu.momentkeep.entity

jwt:
  secret:
    key: ${JWT_SECRET_KEY}
  expiration:
    time: ${JWT_EXPIRATION_TIME:86400000}

deepseek:
  api:
    key: ${DEEPSEEK_API_KEY}
    url: ${DEEPSEEK_API_URL}

binfengyun:
  accessKey: ${BINFENGYUN_ACCESS_KEY}
  secretKey: ${BINFENGYUN_SECRET_KEY}
  bucketName: ${BINFENGYUN_BUCKET_NAME}
  endpoint: ${BINFENGYUN_ENDPOINT}
```

### 4. 端口配置

- 容器端口：8080（已在 Dockerfile 中 EXPOSE）
- 微信云托管会自动将容器端口映射到外部访问地址

### 5. 健康检查配置

在微信云托管控制台配置健康检查：

**HTTP 健康检查**：
- 路径：`/actuator/health`（需要添加 Spring Boot Actuator）
- 端口：8080
- 协议：HTTP
- 检查间隔：30秒
- 超时时间：5秒
- 失败阈值：3次

### 6. 日志配置

微信云托管会自动收集容器日志，建议配置日志输出格式：

```yaml
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  level:
    root: INFO
    cn.edu.scnu.momentkeep: DEBUG
```

## 三、部署步骤

### 1. 代码准备

确保项目根目录包含以下文件：
- `Dockerfile`
- `.dockerignore`
- `pom.xml`
- `src/` 目录

### 2. 微信云托管部署

#### 方式一：通过控制台部署

1. 登录微信云托管控制台
2. 创建新服务
3. 选择「代码部署」方式
4. 上传代码包或连接 Git 仓库
5. 配置环境变量
6. 点击「部署」

#### 方式二：通过 CLI 部署

```bash
# 安装微信云托管 CLI
npm install -g wxcloud-cli

# 登录
wxcloud login

# 部署服务
wxcloud deploy --service momentkeep-backend --path ./backend/MomentKeep
```

### 3. 验证部署

部署完成后，通过以下方式验证：

1. 查看服务状态是否为「运行中」
2. 检查容器日志是否有错误
3. 访问服务地址测试接口

## 四、注意事项

### 1. 数据库连接

- TiDB Cloud 需要配置 SSL 连接
- 建议使用微信云托管的数据库代理服务
- 注意数据库连接池配置

### 2. Redis 配置

- 建议使用微信云托管提供的 Redis 服务
- 配置 Redis 密码保护

### 3. 文件存储

- 用户头像等文件存储在缤纷云（S3兼容）
- 确保配置正确的访问密钥

### 4. 安全配置

- JWT 密钥需要足够复杂
- API 密钥不要硬编码在代码中
- 使用环境变量管理敏感信息

### 5. 性能优化

- 根据实际负载调整 CPU 和内存配置
- 配置合理的自动扩缩容策略
- 启用数据库连接池优化

### 6. 监控告警

- 配置服务监控
- 设置异常告警规则
- 定期查看服务日志

## 五、常见问题

### Q1: 部署失败，构建超时？

**解决方案**：
- 检查 Maven 依赖是否正确
- 使用 `.dockerignore` 减少构建文件
- 配置 Maven 镜像加速

### Q2: 数据库连接失败？

**解决方案**：
- 检查环境变量配置是否正确
- 确认 TiDB Cloud 连接地址和端口
- 检查 SSL 配置

### Q3: 服务启动后无法访问？

**解决方案**：
- 检查端口配置是否正确
- 查看容器日志排查启动错误
- 确认健康检查路径可访问

## 六、联系方式

如有部署问题，请联系：
- 技术支持：MomentKeep Team
- 文档更新：2026-05-24
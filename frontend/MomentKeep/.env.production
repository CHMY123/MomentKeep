# 生产环境变量（H5 / 微信小程序 / App）
#
# H5：与后端同域部署，由 Nginx 把 /api 反向代理到后端 8080 端口，
#     因此使用相对路径，天然没有跨域问题（后端也无需放开 CORS）。
VITE_API_BASE_URL=/api
#
# 微信小程序 / App：必须使用完整 HTTPS 地址。
# 与 H5 共用同一个域名（同域的好处：一张证书、一处备案、H5 零跨域）。
# 前置条件：
#   1) 域名已完成 ICP 备案；
#   2) 已在微信公众平台「开发管理 - 服务器域名」中配置为 request 合法域名；
#   3) 证书为受信任的 HTTPS 证书（不支持 IP 直连与自签证书）。
VITE_API_BASE_URL_MP=https://momentkeep.xyz/api
VITE_API_BASE_URL_APP=https://momentkeep.xyz/api

# MomentKeep 前端开发文档

## 项目概述
MomentKeep（朝暮记）是一个基于Vue3 + uniapp开发的跨平台应用，支持网页端、小程序等多端协同。项目使用TiDB Cloud作为数据库，缤纷云存储用户头像等资源。

## 技术栈
- **前端框架**: Vue 3 (Composition API)
- **跨平台框架**: uniapp
- **UI框架**: uni-ui
- **状态管理**: Pinia
- **网络请求**: uni.request / axios
- **图表库**: ECharts (用于数据可视化)
- **AI集成**: DeepSeek API

## 设计要求

### UI设计（简约文艺+记忆点，拒绝千篇一律的AI感配色）

核心调性：以“时光沉淀、温柔自律”为核心，拒绝大面积高饱和撞色和模板化搭配，用低饱和肌理感配色、留白层次和细节设计，打造有温度、有记忆点的视觉体验，贴合“朝有目标，暮有记录”的slogan，避免冰冷的AI感设计。

#### 1. 配色方案（差异化+贴合调性，拒绝AI感基础配色）

核心逻辑：以“低饱和基础色+小众点缀色”为主，避免纯白+浅蓝/浅绿的AI模板搭配，增加肌理感和层次感，适配多主题模式，同时保证视觉舒适度和可读性。

- **主色调**：雾感米白（#F8F6F2），替代纯白纸面，增加柔和肌理（轻微米纹质感），作为页面主背景，缓解视觉疲劳，贴合“温柔自律”的调性；

- **辅助色**：分两类，拒绝AI常用的浅蓝/浅绿，选用更有记忆点的低饱和色：

  ① 功能辅助色：雾蓝（#94A7C8）、暖灰粉（#D8C8BE），用于按钮、选中状态、图标等，区分功能模块，不刺眼、有质感；

  ② 强调色：焦糖棕（#C2977F），低饱和不突兀，仅用于关键操作（如打卡确认、提交按钮）、数字统计、未读提示，形成视觉焦点，提升记忆点；

- **文字色**：拒绝纯黑，选用深灰（#333333）作为主文字色，中灰（#666666）作为辅助文字，浅灰（#999999）作为提示文字，层级清晰，视觉柔和；

- **主题适配**：深色模式下，主背景为深灰蓝（#1E2228），辅助色同步加深（雾蓝→#4A5568，暖灰粉→#718096），强调色保留焦糖棕（#C2977F），保证对比度和可读性，避免深色模式下的沉闷感。

#### 2. 字体设计（拒绝模板化，贴合文艺调性）

- **主字体**：优先使用系统无衬线字体（iOS：PingFang SC，Android：思源黑体，PC：微软雅黑），保证跨端一致性；

- **字体层级**：明确区分标题、正文、提示文字，拒绝统一字号，增强页面呼吸感：

  ① 页面标题：18px，字重600，焦糖棕，与主背景形成轻微对比，不突兀；

  ② 功能标题（如“每日打卡”“今日待办”）：16px，字重500，深灰，搭配1px细线条下划线（雾蓝色），增强层级；

  ③ 正文文字：14px，字重400，中灰，行高1.6，保证阅读舒适度；

  ④ 提示文字（如表单提示、空状态提示）：12px，字重400，浅灰，避免刺眼；

- **特殊设计**：打卡时间、倒计时数字，选用轻微加粗（字重500），焦糖棕色，突出核心信息，同时不破坏整体简约感。

#### 3. 图标设计（线性+细节，拒绝AI模板化图标）

- **风格**：统一采用线性图标，线条粗细1.5px，圆润边角（圆角2px），避免尖锐线条，贴合整体温柔调性；

- **细节设计**：每个核心功能图标加入微小设计感，拒绝千篇一律的AI图标：

  ① 打卡图标：早起（太阳线条加轻微弧度，不呆板）、睡眠（月亮加细短线表示呼吸感）、用餐（餐具线条简化，避免复杂）、运动（跑步小人线条圆润，有动态感）；

  ② 辅助图标：返回、编辑、删除等基础图标，加入微小肌理（如编辑图标笔尖加细点），增加细节感；

- **状态区分**：未选中图标为中灰，选中/激活状态为焦糖棕，hover状态为雾蓝，颜色过渡自然，避免生硬切换。

#### 4. 布局设计（卡片+留白，拒绝拥挤，突出呼吸感）

- **核心布局**：采用卡片式布局，但拒绝AI常用的“纯白卡片+灰色边框”，优化为：

  ① 卡片样式：雾感米白背景（与页面主背景有轻微色差，#F2EEE8），无明显边框，仅添加1px浅灰阴影（模糊度4px，透明度20%），避免厚重边框的沉闷感；

  ② 留白设计：卡片之间留白20px，卡片内部上下留白16px、左右留白18px，拒绝拥挤布局，提升页面呼吸感；

  ③ 层级设计：页面采用“背景→卡片→功能元素→文字”的四层结构，通过阴影、色差区分层级，不使用多余的分割线，保持简约整洁；

- **特殊布局**：打卡模块、待办列表，采用轻微错落布局（卡片上下错开2px），避免呆板的整齐排列，增加页面灵动性，但不影响整体整洁度。

### 交互设计（柔和+有温度，拒绝机械AI交互）

核心逻辑：交互动作柔和、反馈及时，贴合“记录时光”的温柔调性，避免机械的点击反馈，增加微小的动态细节，让操作有温度，同时保证流畅性。

#### 1. 动画设计（柔和过渡，拒绝生硬切换）

- **过渡动画**：所有页面切换、弹窗弹出/关闭，采用柔和的淡入淡出+轻微平移（Y轴2px），过渡时长300ms，避免快速生硬的切换；

- **操作动画**：

  ① 按钮点击：点击时轻微缩放（0.98倍），同时颜色加深5%，松开后恢复，无明显边框变化，避免机械的“按压凸起”效果；

  ② 打卡操作：打卡成功后，图标从雾蓝变为焦糖棕，同时有轻微的旋转动画（15°），搭配淡色光晕（焦糖棕，透明度30%），反馈更有仪式感；

  ③ 列表操作：待办标记完成、倒计时删除，采用轻微的渐隐渐显动画（时长200ms），避免直接消失的生硬感；

- **加载动画**：拒绝AI常用的圆形旋转加载，设计简约线条加载（3条雾蓝色细线条，交替上下移动），贴合整体线性风格，同时缓解用户等待焦虑。

#### 2. 反馈设计（及时+有温度，拒绝模板化提示）

- **操作反馈**：

  ① 成功反馈：打卡、提交、保存等操作成功后，显示简约提示（无弹窗，仅顶部/底部淡色提示条，雾感米白背景，焦糖棕文字），时长2s，自动消失，不遮挡操作区域；

  ② 错误反馈：表单验证失败、网络错误等，提示条为暖灰粉背景，浅红色文字，搭配微小错误图标（线性，12px），同时聚焦错误字段，方便用户修改；

  ③ hover反馈：菜单项、按钮、列表项hover时，背景色变为雾感米白（#F2EEE8），无明显边框，过渡自然，避免生硬的颜色切换；

- **状态反馈**：未打卡、未完成待办，添加微小的淡色提示（雾蓝，透明度20%），不刺眼，同时明确区分状态，避免用户混淆。

#### 3. 手势设计（贴合多端，优化触控体验）

- **移动端**：

  ① 滑动操作：待办列表支持左滑删除、右滑标记完成，滑动时显示柔和的背景色（删除→暖灰粉，完成→雾蓝），滑动速度适中，避免误触；

  ② 长按操作：长按待办、倒计时，弹出编辑/删除菜单，菜单弹出时有轻微的淡入动画，菜单背景为雾感米白，阴影柔和；

  ③ 点击区域：所有可点击元素（按钮、图标、菜单项）点击区域放大至44px×44px，适配移动端触控，避免误触；

- **PC端**：支持鼠标悬停、滚轮缩放（图表区域），悬停时显示详细信息，滚轮缩放流畅，不卡顿。

#### 4. 响应速度（优化加载逻辑，拒绝等待焦虑）

- **页面加载**：优先加载核心内容（如打卡按钮、待办列表），非核心内容（如历史数据、图表）延迟加载，同时显示简约加载动画，避免页面空白；

- **操作响应**：所有操作（点击、提交、删除）响应时间≤300ms，超过300ms显示加载动画，避免用户误以为操作未生效；

- **图表加载**：打卡数据图表采用渐进式加载（先显示骨架，再加载数据），避免图表空白时间过长。

### 用户体验（个性化+人性化，拒绝AI式模板体验）

核心逻辑：以用户“自律记录”的核心需求为出发点，优化细节体验，让功能更贴合用户习惯，同时保留个性化空间，避免千篇一律的模板化体验。

#### 1. 易用性（简洁直观，降低操作成本）

- **操作流程**：所有核心操作（打卡、新增待办、添加倒计时）不超过3步，避免复杂的表单填写和页面跳转；

- **界面简化**：每个页面核心功能突出，次要功能（如设置、帮助）隐藏在次级菜单，避免界面杂乱；

- **引导设计**：新用户首次进入，采用“分步引导”（仅3步，打卡→待办→倒计时），引导提示为淡色提示条，不强制弹窗，不影响用户操作。

#### 2. 可访问性（适配多场景，兼顾不同用户）

- **屏幕适配**：支持不同屏幕尺寸（从手机到PC），移动端适配全面屏、刘海屏，避免内容被遮挡；

- **字体适配**：支持字体大小调整（设置页可调节，3档：标准、偏大、偏小），兼顾视力不佳用户；

- **颜色适配**：深色模式下，保证文字与背景的对比度≥4.5:1，符合可访问性标准，避免视力疲劳。

#### 3. 一致性（风格统一，避免用户混淆）

- **设计一致性**：所有页面的配色、字体、图标、卡片样式保持统一，避免不同页面风格差异过大；

- **交互一致性**：相同类型的操作（如“新增”“编辑”“删除”）的反馈、手势保持一致，让用户形成操作习惯，降低学习成本；

- **文案一致性**：提示文案、按钮文案风格统一（简洁、温柔，避免生硬的技术话术），如“取消打卡失败”改为“未能取消打卡哦，再试一次吧～”，更有温度。

#### 4. 个性化（拒绝模板化，满足不同用户需求）

- **背景个性化**：除了自定义上传背景，新增3套官方主题背景（雾感森林、简约线条、温柔肌理），均贴合项目调性，避免AI感的高清风景图；

- **打卡个性化**：支持自定义打卡图标（可替换官方线性图标）、打卡提示语（如早起打卡提示语可改为“今日早起，不负时光✨”）；

- **主题个性化**：除了浅色/深色模式，新增“柔和模式”（低饱和度配色，更适合夜间使用），同时支持自定义主题色（仅可修改辅助色，保证整体风格统一）。

## 项目结构
```
MomentKeep/
├── pages/                    # 页面目录
│   ├── login/               # 登录页面
│   ├── register/            # 注册页面
│   ├── index/               # 首页
│   ├── profile/             # 个人中心
│   ├── daily-checkin/       # 每日打卡
│   ├── todo/                # 今日待办
│   ├── countdown/           # 未来倒计时
│   ├── settings/            # 设置
│   └── about/               # 关于我们
├── components/              # 组件目录
│   ├── sidebar/             # 侧边栏
│   ├── checkin-card/        # 打卡卡片
│   ├── todo-item/           # 待办项
│   ├── countdown-card/      # 倒计时卡片
│   └── ai-assistant/        # AI助手侧边栏
├── store/                   # Pinia状态管理
│   ├── user.js             # 用户状态
│   ├── checkin.js          # 打卡状态
│   ├── todo.js             # 待办状态
│   └── settings.js         # 设置状态
├── api/                     # API接口
│   ├── user.js             # 用户相关API
│   ├── checkin.js          # 打卡相关API
│   ├── todo.js             # 待办相关API
│   ├── countdown.js        # 倒计时相关API
│   ├── feedback.js         # 反馈相关API
│   └── ai.js               # AI相关API
├── utils/                   # 工具函数
│   ├── request.js          # 网络请求封装
│   ├── storage.js          # 本地存储封装
│   ├── date.js             # 日期处理
│   └── validator.js        # 表单验证
├── static/                  # 静态资源
└── App.vue                  # 应用入口
```

## 核心功能模块

### 1. 侧边栏 (Sidebar)
**功能描述**:
- 显示圆形用户头像、昵称
- 功能选项卡导航
- 集成AI对话助手侧边栏
- 响应式设计，移动端默认隐藏，通过悬浮按钮控制展开/收起

**组件位置**: `components/Layout.vue`

**关键实现**:
```vue
<template>
  <div class="layout-container">
    <!-- 侧边栏 -->
    <div class="sidebar" :class="{ 'sidebar-active': isSidebarOpen }">
      <!-- 侧边栏内容 -->
    </div>
    
    <!-- 主内容区域 -->
    <div class="main-content">
      <!-- 主内容 -->
    </div>
    
    <!-- 移动端悬浮按钮 -->
    <div class="mobile-toggle" @click="toggleSidebar">
      <div class="toggle-icon"></div>
    </div>
    
    <!-- 遮罩层 -->
    <div v-if="isSidebarOpen && isMobile" class="overlay" @click="closeSidebar"></div>
  </div>
</template>

<script setup>
import { ref, onMounted, onUnmounted } from 'vue'

const isSidebarOpen = ref(false)
const isMobile = ref(false)

// 检测是否为移动端
const checkMobile = () => {
  isMobile.value = window.innerWidth <= 768
  if (isMobile.value) {
    isSidebarOpen.value = false
  }
}

// 切换侧边栏
const toggleSidebar = () => {
  isSidebarOpen.value = !isSidebarOpen.value
}

// 关闭侧边栏
const closeSidebar = () => {
  isSidebarOpen.value = false
}

// 监听窗口大小变化
onMounted(() => {
  checkMobile()
  window.addEventListener('resize', checkMobile)
})

onUnmounted(() => {
  window.removeEventListener('resize', checkMobile)
})
</script>
```

### 2. 个人中心 (Profile)
**功能描述**:
- 用户资料显示和管理
- 用户名（唯一标识）、昵称、邮箱、手机号
- 反馈提交功能（联系方式默认使用手机号）
- 用户注销账户功能
- 修改密码功能

**页面位置**: `pages/profile/`

**核心功能**:
- 田字形布局显示个人资料
- 头像上传（缤纷云存储）和悬停效果
- 资料编辑功能（支持修改昵称、邮箱、手机号）
- 反馈提交表单
- 账户注销确认流程
- 修改密码功能

**关键实现**:
```vue
<template>
  <Layout>
    <div class="profile-container">
      <!-- 用户信息卡片 -->
      <div class="user-card">
        <div class="user-info-header">
          <div class="avatar-section">
            <div class="avatar" @click="uploadAvatar">
              <img :src="userInfo.avatar" alt="avatar" />
              <div class="avatar-edit">
                <div class="icon-camera"></div>
              </div>
            </div>
          </div>
          <div class="edit-btn-container">
            <button class="edit-profile-btn" @click="toggleEditMode">
              {{ isEditMode ? '取消' : '变更资料' }}
            </button>
          </div>
          <div class="user-info-grid">
            <div class="info-item">
              <div class="info-label">用户名</div>
              <div class="info-value">{{ userInfo.username }}</div>
            </div>
            <div class="info-item">
              <div class="info-label">昵称</div>
              <div v-if="!isEditMode" class="info-value">{{ userInfo.nickname }}</div>
              <input v-else type="text" v-model="formData.nickname" placeholder="请输入昵称" />
            </div>
            <div class="info-item">
              <div class="info-label">手机号</div>
              <div v-if="!isEditMode" class="info-value">{{ userInfo.phone }}</div>
              <input v-else type="tel" v-model="formData.phone" placeholder="请输入手机号" />
            </div>
            <div class="info-item">
              <div class="info-label">邮箱</div>
              <div v-if="!isEditMode" class="info-value">{{ userInfo.email }}</div>
              <input v-else type="email" v-model="formData.email" placeholder="请输入邮箱" />
            </div>
          </div>
          <button v-if="isEditMode" class="submit-btn" @click="updateProfile">保存修改</button>
        </div>
      </div>
      
      <!-- 反馈提交 -->
      <div class="form-card">
        <div class="form-title">意见反馈</div>
        <div class="form-item">
          <textarea v-model="feedbackContent" placeholder="请输入您的反馈..." rows="4" />
        </div>
        <div class="form-item">
          <div class="form-label">联系方式</div>
          <div class="contact-info">{{ userInfo.phone }}</div>
        </div>
        <button class="submit-btn" @click="submitFeedback">提交反馈</button>
      </div>
      
      <!-- 账户操作 -->
      <div class="action-card">
        <div class="form-title">账户操作</div>
        <div class="action-item" @click="showChangePasswordDialog">
          <div class="icon-lock"></div>
          <div class="action-text">修改密码</div>
        </div>
        <div class="action-item" @click="showDeleteDialog">
          <div class="icon-close-circle"></div>
          <div class="action-text">注销账户</div>
        </div>
      </div>
    </div>
  </Layout>
</template>

<script setup>
import { ref, reactive, onMounted } from 'vue'
import Layout from '../../components/Layout.vue'

const isDeleteDialogVisible = ref(false)
const isChangePasswordDialogVisible = ref(false)
const isEditMode = ref(false)

// 用户信息
const userInfo = reactive({
  username: 'test',
  nickname: '测试用户',
  email: 'test@example.com',
  phone: '13800138000',
  avatar: 'https://img.icons8.com/ios-filled/50/000000/user.png'
})

// 表单数据
const formData = reactive({
  nickname: '',
  email: '',
  phone: ''
})

// 密码数据
const passwordData = reactive({
  oldPassword: '',
  newPassword: '',
  confirmPassword: ''
})

// 反馈数据
const feedbackContent = ref('')

// 方法
const toggleEditMode = () => {
  isEditMode.value = !isEditMode.value
  if (isEditMode.value) {
    // 进入编辑模式时，初始化表单数据
    Object.assign(formData, userInfo)
  }
}

const uploadAvatar = () => {
  uni.chooseImage({
    count: 1,
    sizeType: ['compressed'],
    sourceType: ['album', 'camera'],
    success: (res) => {
      userInfo.avatar = res.tempFilePaths[0]
      uni.showToast({ title: '头像上传成功', icon: 'success' })
    }
  })
}

const updateProfile = () => {
  Object.assign(userInfo, formData)
  uni.showToast({ title: '资料更新成功', icon: 'success' })
  isEditMode.value = false
}

const submitFeedback = () => {
  if (!feedbackContent.value.trim()) {
    uni.showToast({ title: '请输入反馈内容', icon: 'none' })
    return
  }
  uni.showToast({ title: '反馈提交成功', icon: 'success' })
  feedbackContent.value = ''
}

const showChangePasswordDialog = () => {
  isChangePasswordDialogVisible.value = true
}

const changePassword = () => {
  if (!passwordData.oldPassword) {
    uni.showToast({ title: '请输入当前密码', icon: 'none' })
    return
  }
  if (!passwordData.newPassword) {
    uni.showToast({ title: '请输入新密码', icon: 'none' })
    return
  }
  if (passwordData.newPassword !== passwordData.confirmPassword) {
    uni.showToast({ title: '两次输入的密码不一致', icon: 'none' })
    return
  }
  uni.showToast({ title: '密码修改成功', icon: 'success' })
  isChangePasswordDialogVisible.value = false
}

// 生命周期
onMounted(() => {
  // 初始化表单数据
  Object.assign(formData, userInfo)
})
</script>
```

### 3. 每日打卡 (Daily Check-in)
**功能描述**:
- 四种打卡类型：早起、睡眠、用餐、运动
- 自动获取当前时间并记录
- 用餐打卡支持：早餐、午餐、晚餐、宵夜及自定义
- 运动打卡支持：系统类型选择及自定义输入
- 数据可视化展示

**页面位置**: `pages/daily-checkin/`

**打卡组件**:
```vue
<template>
  <view class="checkin-container">
    <view class="checkin-cards">
      <checkin-card 
        v-for="type in checkinTypes" 
        :key="type.id"
        :type="type"
        @checkin="handleCheckin"
      />
    </view>
    <view class="data-visualization">
      <ec-chart :option="chartOption" />
    </view>
  </view>
</template>
```

**打卡类型配置**:
```javascript
const checkinTypes = [
  { id: 'early', name: '早起打卡', icon: 'sun' },
  { id: 'sleep', name: '睡眠打卡', icon: 'moon' },
  { id: 'meal', name: '用餐打卡', icon: 'food', options: ['早餐', '午餐', '晚餐', '宵夜'] },
  { id: 'exercise', name: '运动打卡', icon: 'sport', options: ['跑步', '健身', '瑜伽', '游泳'] }
]
```

### 4. 今日待办 (Today's Todo)
**功能描述**:
- 编辑今日待办事项
- 提交完成并补充说明
- 首次进入提示是否保留昨日待办

**页面位置**: `pages/todo/`

**核心逻辑**:
```javascript
onMounted(async () => {
  const today = formatDate(new Date())
  const lastVisit = uni.getStorageSync('lastVisitDate')
  
  if (lastVisit !== today) {
    const yesterday = getYesterdayDate()
    const yesterdayTodos = await getTodosByDate(yesterday)
    
    if (yesterdayTodos.length > 0) {
      showKeepYesterdayTodosDialog(yesterdayTodos)
    }
  }
})
```

### 5. 未来倒计时 (Countdown)
**功能描述**:
- 编辑倒计时卡片
- 全局页面右下角显示最近三个倒计时

**页面位置**: `pages/countdown/`

**全局倒计时组件**:
```vue
<template>
  <view class="global-countdown">
    <countdown-card 
      v-for="item in recentCountdowns" 
      :key="item.id"
      :data="item"
    />
  </view>
</template>
```

### 6. 设置 (Settings)
**功能描述**:
- 个性化应用设置
- 主题模式切换（浅色、深色、柔和）
- 字体大小切换（标准、偏大、偏小）
- 网站背景设置（用户上传图片）
- 官方主题背景切换（雾感森林、简约线条、温柔肌理）
- AI助手自动填充提示词开关
- 通知设置（推送通知、打卡提醒）

**页面位置**: `pages/settings/`

**关键实现**:
```vue
<template>
  <Layout>
    <div class="settings-container">
      <!-- 个性化设置 -->
      <div class="setting-section">
        <span class="section-title">个性化设置</span>
        
        <div class="setting-item">
          <span class="setting-label">主题模式</span>
          <div class="setting-value">
            <button 
              v-for="(theme, index) in themes" 
              :key="index"
              :class="['theme-btn', { active: themeIndex === index }]"
              @click="changeTheme(index)"
            >
              {{ theme.name }}
            </button>
          </div>
        </div>
        
        <div class="setting-item">
          <span class="setting-label">字体大小</span>
          <div class="setting-value">
            <button 
              v-for="(size, index) in fontSizes" 
              :key="index"
              :class="['font-size-btn', { active: fontSizeIndex === index }]"
              @click="changeFontSize(index)"
            >
              {{ size }}
            </button>
          </div>
        </div>
        
        <div class="setting-item" @click="chooseBackground">
          <span class="setting-label">背景图片</span>
          <div class="setting-value">
            <span v-if="backgroundImage">已设置</span>
            <span v-else>未设置</span>
            <div class="arrow-icon"></div>
          </div>
        </div>
        
        <div class="setting-item">
          <span class="setting-label">官方主题背景</span>
          <div class="setting-value">
            <div class="theme-backgrounds">
              <div 
                v-for="(theme, index) in themeBackgrounds" 
                :key="index"
                :class="['theme-background-item', { active: selectedTheme === index }]"
                @click="selectThemeBackground(index)"
              >
                <img :src="theme.image" :alt="theme.name" />
                <span class="theme-name">{{ theme.name }}</span>
              </div>
            </div>
          </div>
        </div>
      </div>
      
      <!-- AI设置 -->
      <div class="setting-section">
        <span class="section-title">AI设置</span>
        
        <div class="setting-item">
          <span class="setting-label">AI自动填充提示词</span>
          <div class="setting-value">
            <label class="switch">
              <input type="checkbox" :checked="aiAutoFill" @change="toggleAiAutoFill" />
              <span class="switch-slider"></span>
            </label>
          </div>
        </div>
        
        <div class="setting-item">
          <span class="setting-label">今日内免弹出</span>
          <div class="setting-value">
            <label class="switch">
              <input type="checkbox" :checked="todayNoPopup" @change="toggleTodayNoPopup" />
              <span class="switch-slider"></span>
            </label>
          </div>
        </div>
      </div>
      
      <!-- 通知设置 -->
      <div class="setting-section">
        <span class="section-title">通知设置</span>
        
        <div class="setting-item">
          <span class="setting-label">推送通知</span>
          <div class="setting-value">
            <label class="switch">
              <input type="checkbox" :checked="notifications" @change="toggleNotifications" />
              <span class="switch-slider"></span>
            </label>
          </div>
        </div>
        
        <div class="setting-item">
          <span class="setting-label">打卡提醒</span>
          <div class="setting-value">
            <label class="switch">
              <input type="checkbox" :checked="checkinReminder" @change="toggleCheckinReminder" />
              <span class="switch-slider"></span>
            </label>
          </div>
        </div>
      </div>
    </div>
  </Layout>
</template>

<script setup>
import { ref, reactive, onMounted } from 'vue'
import Layout from '../../components/Layout.vue'

// 主题设置
const themeIndex = ref(0) // 0: 浅色, 1: 深色, 2: 柔和
const fontSizeIndex = ref(0) // 0: 标准, 1: 偏大, 2: 偏小
const backgroundImage = ref('')
const selectedTheme = ref(0)

// AI设置
const aiAutoFill = ref(true)
const todayNoPopup = ref(false)

// 通知设置
const notifications = ref(true)
const checkinReminder = ref(true)

// 主题选项
const themes = [
  { name: '浅色' },
  { name: '深色' },
  { name: '柔和' }
]

// 字体大小选项
const fontSizes = ['标准', '偏大', '偏小']

// 官方主题背景
const themeBackgrounds = [
  {
    name: '雾感森林',
    image: 'https://img.icons8.com/color/200/000000/forest.png'
  },
  {
    name: '简约线条',
    image: 'https://img.icons8.com/color/200/000000/line.png'
  },
  {
    name: '温柔肌理',
    image: 'https://img.icons8.com/color/200/000000/nature.png'
  }
]

// 方法
const changeTheme = (index) => {
  themeIndex.value = index
  localStorage.setItem('themeIndex', index.toString())
  applyTheme(index)
  uni.showToast({ title: '主题已更新', icon: 'success' })
}

const changeFontSize = (index) => {
  fontSizeIndex.value = index
  localStorage.setItem('fontSizeIndex', index.toString())
  applyFontSize(index)
  uni.showToast({ title: '字体大小已更新', icon: 'success' })
}

const chooseBackground = () => {
  uni.chooseImage({
    count: 1,
    sizeType: ['compressed'],
    sourceType: ['album', 'camera'],
    success: (res) => {
      backgroundImage.value = res.tempFilePaths[0]
      localStorage.setItem('backgroundImage', res.tempFilePaths[0])
      applyBackgroundImage(res.tempFilePaths[0])
      uni.showToast({ title: '背景图片已设置', icon: 'success' })
    }
  })
}

const selectThemeBackground = (index) => {
  selectedTheme.value = index
  localStorage.setItem('selectedTheme', index.toString())
  applyThemeBackground(index)
  uni.showToast({ title: '主题背景已更新', icon: 'success' })
}

// 应用主题
const applyTheme = (index) => {
  const themes = {
    0: { // 浅色
      '--bg-color': '#F8F6F2',
      '--text-color': '#333333',
      '--primary-color': '#C2977F',
      '--secondary-color': '#94A7C8',
      '--sidebar-bg': '#F2EEE8',
      '--sidebar-border': 'rgba(0, 0, 0, 0.05)'
    },
    1: { // 深色
      '--bg-color': '#2D2D2D',
      '--text-color': '#E5E5E5',
      '--primary-color': '#D8C8BE',
      '--secondary-color': '#6B7280',
      '--sidebar-bg': '#1E1E1E',
      '--sidebar-border': 'rgba(255, 255, 255, 0.1)'
    },
    2: { // 柔和
      '--bg-color': '#F0EAE1',
      '--text-color': '#4A4A4A',
      '--primary-color': '#A8846B',
      '--secondary-color': '#8A9BB0',
      '--sidebar-bg': '#E8E1D6',
      '--sidebar-border': 'rgba(0, 0, 0, 0.05)'
    }
  }
  
  const theme = themes[index]
  for (const [key, value] of Object.entries(theme)) {
    document.documentElement.style.setProperty(key, value)
  }
}

// 应用字体大小
const applyFontSize = (index) => {
  const sizes = {
    0: '16px', // 标准
    1: '18px', // 偏大
    2: '14px'  // 偏小
  }
  document.documentElement.style.fontSize = sizes[index]
}

// 应用背景图片
const applyBackgroundImage = (image) => {
  document.body.style.backgroundImage = `url(${image})`
  document.body.style.backgroundSize = 'cover'
  document.body.style.backgroundPosition = 'center'
  document.body.style.backgroundRepeat = 'no-repeat'
}

// 应用官方主题背景
const applyThemeBackground = (index) => {
  const theme = themeBackgrounds[index]
  document.body.style.backgroundImage = `url(${theme.image})`
  document.body.style.backgroundSize = 'cover'
  document.body.style.backgroundPosition = 'center'
  document.body.style.backgroundRepeat = 'no-repeat'
}

// 生命周期
onMounted(() => {
  // 从本地存储加载设置
  const savedTheme = localStorage.getItem('themeIndex')
  const savedFontSize = localStorage.getItem('fontSizeIndex')
  const savedBackground = localStorage.getItem('backgroundImage')
  const savedThemeBackground = localStorage.getItem('selectedTheme')
  
  if (savedTheme) {
    themeIndex.value = parseInt(savedTheme)
    applyTheme(themeIndex.value)
  }
  
  if (savedFontSize) {
    fontSizeIndex.value = parseInt(savedFontSize)
    applyFontSize(fontSizeIndex.value)
  }
  
  if (savedBackground) {
    backgroundImage.value = savedBackground
    applyBackgroundImage(savedBackground)
  }
  
  if (savedThemeBackground) {
    selectedTheme.value = parseInt(savedThemeBackground)
    applyThemeBackground(selectedTheme.value)
  }
})
</script>
```

### 7. 登录与注册 (Login & Register)
**功能描述**:
- 用户登录功能
- 用户注册功能
- 表单验证
- 错误提示

**页面位置**: `pages/login/` 和 `pages/register/`

**关键实现**:
```vue
<template>
  <div class="login-container">
    <div class="login-card">
      <div class="logo-container">
        <img src="../../static/logo.png" alt="朝暮记" class="logo" />
        <h1 class="app-name">朝暮记</h1>
        <p class="app-slogan">朝有目标，暮有记录</p>
      </div>
      
      <form @submit.prevent="handleLogin">
        <div class="form-item">
          <label for="username">用户名</label>
          <input 
            type="text" 
            id="username" 
            v-model="formData.username" 
            placeholder="请输入用户名" 
            required
          />
        </div>
        
        <div class="form-item">
          <label for="password">密码</label>
          <input 
            type="password" 
            id="password" 
            v-model="formData.password" 
            placeholder="请输入密码" 
            required
          />
        </div>
        
        <button type="submit" class="login-btn" :disabled="loading">
          {{ loading ? '登录中...' : '登录' }}
        </button>
        
        <div class="register-link">
          <span>还没有账号？</span>
          <a @click="goToRegister">立即注册</a>
        </div>
      </form>
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { useUserStore } from '../../store/user'

const router = useRouter()
const userStore = useUserStore()

const formData = ref({
  username: '',
  password: ''
})

const loading = ref(false)

const handleLogin = async () => {
  if (!formData.value.username || !formData.value.password) {
    uni.showToast({ title: '请输入用户名和密码', icon: 'none' })
    return
  }
  
  loading.value = true
  
  try {
    const response = await uni.request({
      url: 'http://localhost:8080/api/user/login',
      method: 'POST',
      data: formData.value
    })
    
    if (response.data.code === 200) {
      const { token, user } = response.data.data
      userStore.setUser(user)
      userStore.setToken(token)
      uni.showToast({ title: '登录成功', icon: 'success' })
      router.push('/pages/index/index')
    } else {
      uni.showToast({ title: response.data.message, icon: 'none' })
    }
  } catch (error) {
    uni.showToast({ title: '登录失败，请稍后重试', icon: 'none' })
  } finally {
    loading.value = false
  }
}

const goToRegister = () => {
  router.push('/pages/register/register')
}
</script>
```

### 8. 关于我们 (About)
**功能描述**:
- 项目基本信息
- 开发者信息

**页面位置**: `pages/about/`

### 9. AI对话助手 (AI Assistant)
**功能描述**:
- 集成DeepSeek API
- 根据用户打卡情况自动填充提示词
- 进入侧边栏时提示是否使用（今日内免弹出）
- 可在设置中关闭

**组件位置**: `components/ai-assistant/`

**核心功能**:
```javascript
async function openAIAssistant() {
  const settings = settingsStore.getSettings()
  
  if (settings.aiAutoFill && !hasShownPromptToday()) {
    const useAutoFill = await showAutoFillPrompt()
    
    if (useAutoFill) {
      const checkinData = await getTodayCheckinData()
      const prompt = generatePromptFromCheckin(checkinData)
      aiStore.setPrompt(prompt)
    }
    
    setPromptShownToday()
  }
  
  showAIAssistant()
}
```

## 状态管理 (Pinia)

### 用户状态 (user.js)
```javascript
export const useUserStore = defineStore('user', {
  state: () => ({
    userInfo: null,
    isLoggedIn: false
  }),
  actions: {
    async login(credentials) { },
    async logout() { },
    async updateProfile(data) { },
    async uploadAvatar(file) { }
  }
})
```

### 打卡状态 (checkin.js)
```javascript
export const useCheckinStore = defineStore('checkin', {
  state: () => ({
    todayCheckins: [],
    checkinHistory: []
  }),
  actions: {
    async recordCheckin(type, data) { },
    async getTodayCheckins() { },
    async getCheckinStats() { }
  }
})
```

### 待办状态 (todo.js)
```javascript
export const useTodoStore = defineStore('todo', {
  state: () => ({
    todayTodos: [],
    completedTodos: []
  }),
  actions: {
    async addTodo(todo) { },
    async completeTodo(id, note) { },
    async getTodosByDate(date) { }
  }
})
```

### 设置状态 (settings.js)
```javascript
export const useSettingsStore = defineStore('settings', {
  state: () => ({
    theme: 'light',
    backgroundImage: '',
    aiAutoFill: true,
    notifications: true
  }),
  actions: {
    async updateSettings(settings) { },
    async uploadBackground(file) { }
  }
})
```

## API接口封装

### 网络请求封装 (utils/request.js)
```javascript
const baseURL = 'https://api.momentkeep.com'

function request(options) {
  return new Promise((resolve, reject) => {
    uni.request({
      url: baseURL + options.url,
      method: options.method || 'GET',
      data: options.data,
      header: {
        'Authorization': `Bearer ${uni.getStorageSync('token')}`,
        'Content-Type': 'application/json'
      },
      success: (res) => {
        if (res.statusCode === 200) {
          resolve(res.data)
        } else {
          reject(res)
        }
      },
      fail: reject
    })
  })
}

export default request
```

### 用户API (api/user.js)
```javascript
import request from '@/utils/request'

export const userApi = {
  login(data) {
    return request({
      url: '/api/user/login',
      method: 'POST',
      data
    })
  },
  
  getProfile() {
    return request({
      url: '/api/user/profile',
      method: 'GET'
    })
  },
  
  updateProfile(data) {
    return request({
      url: '/api/user/profile',
      method: 'PUT',
      data
    })
  },
  
  uploadAvatar(file) {
    return request({
      url: '/api/user/avatar',
      method: 'POST',
      data: { file }
    })
  },
  
  submitFeedback(data) {
    return request({
      url: '/api/user/feedback',
      method: 'POST',
      data
    })
  },
  
  logout() {
    return request({
      url: '/api/user/logout',
      method: 'POST'
    })
  },
  
  deleteAccount(data) {
    return request({
      url: '/api/user/delete',
      method: 'POST',
      data
    })
  }
}
```

### 打卡API (api/checkin.js)
```javascript
import request from '@/utils/request'

export const checkinApi = {
  recordCheckin(data) {
    return request({
      url: '/api/checkin/record',
      method: 'POST',
      data
    })
  },
  
  getTodayCheckins() {
    return request({
      url: '/api/checkin/today',
      method: 'GET'
    })
  },
  
  getCheckinStats(params) {
    return request({
      url: '/api/checkin/stats',
      method: 'GET',
      data: params
    })
  }
}
```

### AI API (api/ai.js)
```javascript
import request from '@/utils/request'

export const aiApi = {
  chat(messages) {
    return request({
      url: '/api/ai/chat',
      method: 'POST',
      data: { messages }
    })
  },
  
  getCheckinPrompt() {
    return request({
      url: '/api/ai/prompt/checkin',
      method: 'GET'
    })
  }
}
```

## 数据可视化

### ECharts配置示例
```javascript
const chartOption = {
  title: {
    text: '打卡时间分布'
  },
  tooltip: {
    trigger: 'axis'
  },
  legend: {
    data: ['早起', '睡眠', '用餐', '运动']
  },
  xAxis: {
    type: 'category',
    data: ['周一', '周二', '周三', '周四', '周五', '周六', '周日']
  },
  yAxis: {
    type: 'value'
  },
  series: [
    {
      name: '早起',
      type: 'line',
      data: []
    },
    {
      name: '睡眠',
      type: 'line',
      data: []
    }
  ]
}
```

## 多端适配

### 条件编译示例
```javascript
// #ifdef H5
console.log('网页端')
// #endif

// #ifdef MP-WEIXIN
console.log('微信小程序')
// #endif

// #ifdef APP-PLUS
console.log('APP端')
// #endif
```

## 开发规范

### 命名规范
- 组件名：PascalCase (如 `CheckinCard`)
- 文件名：kebab-case (如 `checkin-card.vue`)
- 变量名：camelCase (如 `userInfo`)
- 常量名：UPPER_SNAKE_CASE (如 `API_BASE_URL`)

### 代码风格
- 使用Composition API
- 使用`<script setup>`语法
- 组件props必须定义类型
- 使用TypeScript进行类型检查

### Git提交规范
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具链相关
```

## 部署说明

### 网页端部署
```bash
# 构建H5
npm run build:h5

# 输出目录: dist/build/h5
# 部署到静态服务器
```

### 小程序部署
```bash
# 构建微信小程序
npm run build:mp-weixin

# 输出目录: dist/build/mp-weixin
# 使用微信开发者工具打开并上传
```

### APP部署
```bash
# 构建APP
npm run build:app

# 输出目录: dist/build/app
# 使用HBuilderX打包
```

## 注意事项

1. **跨平台兼容性**: 使用uniapp的条件编译处理平台差异
2. **状态持久化**: 使用uni.storage进行本地数据持久化
3. **图片上传**: 使用缤纷云存储API上传头像和背景图
4. **AI调用**: 注意DeepSeek API的调用频率限制
5. **数据同步**: 确保多端数据实时同步
6. **性能优化**: 合理使用缓存，减少不必要的网络请求

## 开发环境配置

### 环境变量
```env
# .env.development
VITE_API_BASE_URL=http://localhost:8080
VITE_DEEPSEEK_API_KEY=your_dev_key

# .env.production
VITE_API_BASE_URL=https://api.momentkeep.com
VITE_DEEPSEEK_API_KEY=your_prod_key
```

### 依赖安装
```bash
npm install
npm run dev:h5        # 开发网页端
npm run dev:mp-weixin # 开发微信小程序
npm run dev:app       # 开发APP
```

## 测试计划

### 单元测试
- 组件测试
- 工具函数测试
- API接口测试

### 集成测试
- 页面跳转测试
- 状态管理测试
- 数据同步测试

### 端到端测试
- 用户流程测试
- 多端协同测试

## 性能优化

1. **代码分割**: 使用动态import按需加载
2. **图片优化**: 使用懒加载和图片压缩
3. **缓存策略**: 合理使用本地缓存
4. **请求优化**: 合并请求，使用防抖节流
5. **渲染优化**: 虚拟列表处理长列表

## 安全考虑

1. **数据加密**: 敏感数据传输加密
2. **身份验证**: JWT token验证
3. **输入验证**: 前端表单验证
4. **XSS防护**: 使用v-html时注意转义
5. **CSRF防护**: 使用token验证

## 后续扩展

1. **主题系统**: 支持更多主题切换
2. **国际化**: 多语言支持
3. **离线模式**: 支持离线使用
4. **数据导出**: 支持导出用户数据
5. **社交功能**: 好友系统和分享功能
